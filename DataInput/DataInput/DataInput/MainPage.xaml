<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:DataInput"
             x:Class="DataInput.MainPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackLayout Grid.Row="0" HorizontalOptions="FillAndExpand" VerticalOptions="Center">
            <Entry x:Name="Email" Placeholder="Email" Keyboard="Email" Margin="10"/>
            <Entry x:Name="Website" Placeholder="Website" Keyboard="Url" Margin="10"/>
            <Entry x:Name="Telephone" Placeholder="Telephone" Keyboard="Telephone" Margin="10"/>
        </StackLayout>
        <Grid Grid.Row="1" Margin="10">
            <StackLayout Orientation="Horizontal" HorizontalOptions="Center">
                <Button Text="New" WidthRequest="100" Clicked="New_Clicked"/>
                <Button Text="Open" WidthRequest="100" Clicked="Open_Clicked"/>
                <Button Text="Save" WidthRequest="100" Clicked="Save_Clicked"/>
            </StackLayout>
        </Grid>
    </Grid>

</ContentPage>
    